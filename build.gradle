/*
 * This file was generated by the Gradle 'init' task.
 */

plugins {
    id 'java'
    id 'maven-publish'
    id 'java-gradle-plugin'
}

apply plugin: 'maven'

repositories {
    mavenLocal()
    maven {
        url = 'http://repo.maven.apache.org/maven2'
    }
}

dependencies {
    compile 'junit:junit:4.12'
    compile 'org.davidespadini:tool-mockextractor:0.0.1-SNAPSHOT'
    compile gradleApi()
}

group = 'com.github.jazzmuesli'
version = '1.0-SNAPSHOT'
description = 'mockextractor-gradle-plugin'
sourceCompatibility = '1.8'

gradlePlugin {
    plugins {
        ckGradlePlugin {
            id = 'MeGradlePlugin'
            implementationClass = 'com.github.jazzmuesli.mockextractor.gradle.plugin.MockExtractorPlugin'
        }
    }
}

task sourcesJar(type: Jar) {
    classifier = 'sources'
    from(sourceSets.main.allJava)
}

publishing {
    publications {
        maven(MavenPublication) {
            from(components.java)
            artifact(sourcesJar)
        }
    }
}

tasks.withType(JavaCompile) {
    options.encoding = 'UTF-8'
}
